<van-panel title="登录" border="{{ false }}" class="login-panel">
    <van-cell-group border="{{ false }}" class="field-cells">
        <van-field
            model:value="{{ username }}"
            placeholder="请输入用户名"
        />
        <van-field
            model:value="{{ password }}"
            placeholder="请输入密码"
        />
    </van-cell-group>
    <van-button 
        class="login-button"
        type="primary"
        bind:change="onSubmit"
    >登录</van-button>
</van-panel>